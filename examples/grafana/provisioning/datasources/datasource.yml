apiVersion: 1

datasources:
  - name: prod
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    uid: prometheus-uid-prod
    basicAuth: false
    jsonData:
      timeInterval: "30s"